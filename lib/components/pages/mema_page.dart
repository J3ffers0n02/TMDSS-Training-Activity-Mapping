import 'package:flutter/material.dart';

class MemaPage extends StatelessWidget {
  const MemaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('MemaPage'),),);
  }
}